#pragma once
#include <cstring>
#include <cstdlib>
// Error codes referenced by AnmManager (values irrelevant; never hit at runtime).
enum {
    TH_ERR_ANMMANAGER_SPRITE_CORRUPTED = 100,
    TH_ERR_ANMMANAGER_TEXTURE_CORRUPTED = 101,
};
